# Test ortam? için application properties

# Elasticsearch test konfigürasyonu
spring.elasticsearch.uris=http://localhost:9200

# Restaurant API test URL
restaurant.api.url=http://localhost:8081

# Delivery API test URL
delivery.api.url=http://localhost:8082

# Logging seviyesi
logging.level.com.example.orderapi=DEBUG
logging.level.org.springframework.data.elasticsearch=DEBUG

# Test için HTTP client timeout ayarlar?
spring.http.client.timeout.connect=5s
spring.http.client.timeout.read=10s
